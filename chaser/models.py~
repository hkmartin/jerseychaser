from django.db import models

class Chaser(models.Model):
    name = models.CharField(max_length=255)
    year_in_school = models.CharField(max_length=50)
    def __unicode__(self):
        return self.name
